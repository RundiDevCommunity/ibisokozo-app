import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ng-ibisokozo-app',
  webDir: 'dist/ng-ibisokozo-app',
  server: {
    androidScheme: 'https'
  }
};

export default config;
